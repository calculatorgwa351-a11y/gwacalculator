services:
  - type: web
    name: gwacalculator
    runtime: python
    region: oregon
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python init.py && uvicorn app.main:app --host 0.0.0.0 --port $PORT --workers 2
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.5
      - key: SECRET_KEY
        generateValue: true
      # Supabase PostgreSQL Configuration
      - key: PGUSER
        value: postgres.orzouvypfqrrerciryoy
      - key: PGPASSWORD
        value: VnrPNtYCDjiPDjB5
      - key: PGHOST
        value: aws-1-ap-southeast-2.pooler.supabase.com
      - key: PGPORT
        value: 6543
      - key: PGDATABASE
        value: postgres
      - key: SUPABASE_SSL_NO_VERIFY
        value: 1
